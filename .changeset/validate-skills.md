---
"nikhil-skills": patch
---

Add `scripts/validate-skills` (bash, checks frontmatter + duplicate names) wired up as
a `hooks/pre-commit` hook, and document why in `.agents/adr/0005-bash-pre-commit-validation-no-release-bot-yet.md`.
