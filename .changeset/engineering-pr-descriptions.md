---
"nikhil-skills": minor
---

Add `pr-descriptions` to `engineering/`: model-invoked authoring of high-signal PR
bodies structured as Problem → Solution → Architecture → Testing, with mermaid diagram
rules, real-over-invented code snippets, GitHub alert callouts, and PR-train
conventions (TOC, position narrative, per-branch CI-green, no cross-PR churn,
SHA-pinned file tables). Adds repo naming conventions (`[JIRA-XXX]` PR titles,
`<username>-YYYYMMDD-camelCaseDescription` branches, trailing `JIRA:` footer) and ships
a standalone `PR_TEMPLATE.md` for repos without a checked-in template. Original to
this repo — no upstream metadata block.
